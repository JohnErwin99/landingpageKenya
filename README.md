# landingpageKenya

Iristel Kenya campaign landing page — single-file static HTML (`index.html`).

## Development

Open `index.html` in a browser, or serve locally:

```bash
python3 -m http.server 8000
```
